{
    "files.exclude": {
        "**/*.js": { "when": "$(basename).ts" },
        "**/**.js": { "when": "$(basename).tsx" },
        "**/*.js.map": { "when": "$(basename)" },
        "package-lock.json": true,
        //"node_modules": true,
        "index.js": true,
    }
}